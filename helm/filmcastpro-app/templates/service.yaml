apiVersion: v1
kind: Service
metadata:
  name: {{ include "filmcastpro-app.fullname" . }}
  labels:
    {{- include "filmcastpro-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "filmcastpro-app.selectorLabels" . | nindent 4 }}
